<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.ssafy.happyhouse.model.dao.LoginDao">

	<select id="login" parameterType="String" resultType="com.ssafy.happyhouse.model.dto.UserDtos">
		select userSeq, userName, userPassword, userEmail, userProfileImageUrl, userRegisterDate, userMessage, userRank
		from user
		where userEmail = #{userEmail} 
	</select>

</mapper>


