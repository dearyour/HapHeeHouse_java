<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.dao.HouseDealDAO">
	<select id="searchDealInfoByAptCode" parameterType="int" resultType="houseDealDto">
		select no, (select aptName from houseinfo i where i.aptCode=#{aptCode}) aptName, aptCode, dealAmount, dealYear, dealMonth, dealDay, area, floor, type, rentMoney 
		from housedeal
		where aptCode=#{aptCode}
		order by no desc
	</select>
</mapper>